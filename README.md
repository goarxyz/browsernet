# browsernet
Client-side Software-Defined Network for Go WASM. Intercepts net.Dial/Resolver/Listen and tunnels TCP/UDP/TLS through SOCKS5 over WebSocket and WebRTC so unmodified Go networking works in the browser on your own infrastructure.
